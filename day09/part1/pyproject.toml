[tool.poetry]
name = "part1"
version = "0.1.0"
description = ""
authors = ["Rohrschacht"]

[tool.poetry.dependencies]
python = "^3.10"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
